---
import BaseLayout from '../../components/BaseLayout.astro';
import { LandingExperience } from '../../components/LandingExperience';
import { COPY, SUPPORTED_LOCALES, localizedPath, type Locale } from '../../lib/i18n';

import HeroCarousel from '../../components/HeroCarousel';

export function getStaticPaths() {
  return SUPPORTED_LOCALES.map((locale) => ({
    params: { locale },
  }));
}

const { locale } = Astro.params;
const INSTALL_URL = 'https://apps.apple.com/app/id6787150982';
const copy = COPY[locale as Locale];
const privacyHref = localizedPath(locale as Locale, "/policy");
---

<BaseLayout locale={locale as Locale} copy={copy}>
  <section class="hero" aria-labelledby="hero-title">
    <LandingExperience client:load installUrl={INSTALL_URL} privacyHref={privacyHref} copy={copy} />
    <div class="hero-media reveal-phone" aria-label="SummaFit app preview">
      <div class="phone-shadow" aria-hidden="true" />
      <HeroCarousel client:load />
    </div>
  </section>
  <section class="sr-only">
    <h2>{copy.seoTitle}</h2>
    <p>{copy.seoDescription}</p>
    <h3>{copy.title}</h3>
    <p>{copy.subtitle}</p>
  </section>
</BaseLayout>
